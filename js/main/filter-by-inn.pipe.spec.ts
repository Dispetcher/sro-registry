import { FilterByINNPipe } from './filter-by-inn.pipe';

describe('FilterByINNPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByINNPipe();
    expect(pipe).toBeTruthy();
  });
});
