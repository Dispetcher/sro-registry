import { FilterByOGRNPipe } from './filter-by-ogrn.pipe';

describe('FilterByOGRNPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByOGRNPipe();
    expect(pipe).toBeTruthy();
  });
});
