import { FilterByNumPipe } from './filter-by-num.pipe';

describe('FilterByNumPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNumPipe();
    expect(pipe).toBeTruthy();
  });
});
